<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tech.redltd.lmsCustomer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".base.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.applyLoan.ApplyLoanActivity"></activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity
            android:name=".activities.UpdateProfileActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activities.loanStatus.LoanStatusActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activities.loanList.LoanListActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activities.installmentCheck.InstallmentCheckActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activities.profileActivity.ProfileActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activities.checkEligibility.CheckEligibilityActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activities.HomeActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activities.RegisterSuccessActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activities.otpActivity.OtpActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activities.loginActivity.LoginActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activities.createAccount.CreateAccountActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activities.mainActivity.MainActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activities.SplashScreenActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>